# Language setting
language: node_js

# Version number
node_js:
  - 0.10.25

build_image: dockerfile/nodejs

before_script:
  - npm install gulp -g
  - npm install
  - bower install -f

# Running the tests with grunt
script:
  - gulp build

cache:true